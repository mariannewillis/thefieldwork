{
  "name": "thefieldwork",
  "version": "0.1.0",
  "private": true,
  "description": "The Field Work — aura healing practice site and admin portal. The Next.js app lives in app/.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "start": "npm --prefix app run start",
    "lint": "npm --prefix app run lint",
    "typecheck": "npm --prefix app run typecheck",
    "install:app": "npm --prefix app ci"
  }
}
